const res = await fetch('/api/submitTour', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(payload)
});

let data = {};
try { data = await res.json(); } catch { /* okay if no JSON */ }

if (res.ok) {
  // show success toast/state
  showSuccess('Request submitted! We’ll email you after approval.');
} else {
  const msg = data?.error || `HTTP ${res.status}`;
  showError(`Oops: ${msg}`);
}
